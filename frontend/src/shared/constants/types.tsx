export interface IFilms {
    title: string
    description: string
    post: string
    date: string
    country: string
    actor: number
    category: number
    preview: string
    genres: number[]
}